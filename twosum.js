var twoSum = function(numbers, target) {
  // let obj = Object.assign({}, numbers)
  numbers
  // return [numbers.indexOf(1) + 1, numbers.indexOf(3) + 1 ]
  return false
};

console.assert(twoSum([1,3,5,6,7], 6))